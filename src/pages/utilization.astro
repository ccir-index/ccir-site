---
// /utilization has moved to /availability — the supply page was renamed when the
// implied-blend "utilization" headline was superseded by the curated Availability
// Index (breadth of genuine-capacity venues). Measured Vast utilization now lives
// on /availability as the depth card. Static-host-safe meta-refresh redirect.
const DEST = '/availability';
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="refresh" content={`0; url=${DEST}`} />
    <link rel="canonical" href={DEST} />
    <meta name="robots" content="noindex" />
    <title>Moved to Availability · CCIR</title>
  </head>
  <body>
    <p>This page has moved to <a href={DEST}>{DEST}</a>.</p>
    <script set:html={`location.replace(${JSON.stringify(DEST)});`}></script>
  </body>
</html>
