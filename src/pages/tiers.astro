---
// /tiers is retired — the operator-segment definitions now live in the
// TierDefinitions block on /rates. Permanent-redirect anything that still
// points here.
return Astro.redirect('/rates', 301);
---
